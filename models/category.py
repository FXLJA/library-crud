# Class ini digunakan untuk menyimpan data Category dari database library_0082
class Category:
    # Constructor ini digunakan untuk inisialiasi data dari class Category
    def __init__(self, category_id, category_name):
        self.category_id = category_id
        self.category_name = category_name
